#include<stdio.h>
#include<conio.h>
int main ()
{
	printf("Enter the number to be checked");
	int num ;
	scanf("%d", &num);
if(num %2 == 0)
{ 
  printf("the number is even");
	}
	else
	{ 
	 printf("the number is odd");
		}
		return 0;
		
}
